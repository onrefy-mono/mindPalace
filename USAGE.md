# 思维宫殿 · 使用指南

个人工作记忆与思维可视化工具。左侧管理「当前关注」，右侧以 D3 力导向图展示思维网络。

---

## 启动

```bash
cd D:\azeProjects\Projects\mind-palace
npm install
npm run dev
```

浏览器打开终端显示的地址（如 `http://localhost:5173`）。

---

## 界面概览

```
┌──────────────────────────────────────────────────────────────────────────┐
│  思维宫殿                              [快速捕获] [导出] [重置]            │
├──────────┬────────────┬─────────────────────────────┬───────────────────┤
│  关注区   │  关注详情   │      思维图谱（D3）           │    节点详情        │
│          │            │  [图例]  力导向网络            │                   │
│  点击卡片 │  备注/关联  │  双击空白 → 创建节点          │  分类/编辑/删除    │
└──────────┴────────────┴─────────────────────────────┴───────────────────┘
```

| 区域 | 作用 |
|------|------|
| **关注区** | 记录你现在在想什么（研究 / 工作 / 个人） |
| **关注详情** | 选中关注项后，在关注区右侧显示备注、活跃度、关联节点 |
| **思维图谱** | 展示概念节点与关系，左上角有分类图例 |
| **节点详情** | 选中节点后，在图谱右侧显示分类、内容、关联，可编辑/删除 |

---

## 关注区

### 添加关注点

1. 点击左下角 **「+ 添加关注点」**
2. 填写标题、选择域（研究 / 工作 / 个人）、可选备注
3. 点击 **添加**

系统会**自动创建一个「项目」类节点**（标签 `#focus`）并关联到该关注点，图谱中会同步高亮。

### 切换当前关注

- **点击**某个关注卡片 → 图谱会高亮其关联节点，并自动聚焦到相关区域

### 卡片操作

| 按钮 | 功能 |
|------|------|
| **📌** | 固定关注点，防止因久未触碰而衰减降权 |
| **✓** | 标记为已完成，移入「已完成」分组 |

### 排序规则

关注区按综合分数自动排序，影响因素：

- **优先级**（1 最高）
- **是否固定**
- **最近触碰时间**（越久未访问，排名越低）
- **触碰次数**（多次访问更稳定）

卡片底部的彩色进度条反映当前活跃程度。

---

## 思维图谱

### 视觉含义

| 形状 | 分类 | 心理学维度 |
|------|------|-----------|
| 圆形 | 概念 | 语义记忆 |
| 三角形 | 问题 | 认知缺口 |
| 菱形 | 决策 | 执行功能 |
| 圆角矩形 | 项目 | 目标系统 |
| 六边形 | 人物 | 社会认知 |
| 五角星 | 洞察 | 整合顿悟 |
| 椭圆 | 经验 | 情节记忆 |

图谱左上角有完整图例。关注节点仍会发光高亮，邻居节点次级高亮。

### 交互

| 操作 | 效果 |
|------|------|
| **拖拽节点** | 调整布局（松手后力导向继续生效） |
| **滚轮** | 缩放图谱 |
| **拖拽空白** | 框选多个节点 / Box，并替换当前选择 |
| **Shift + 拖拽空白** | 追加框选到当前选择 |
| **Ctrl/Cmd + 拖拽空白** | 从当前选择中移除框选对象 |
| **Alt + 拖拽空白** | 平移画布 |
| **单击节点** | 选中，右侧节点详情面板即时编辑 |
| **Shift + 单击** | 多选节点 |
| **双击空白** | 打开创建节点面板 |
| **Tab** | 打开快速创建菜单（按分类） |
| **Ctrl+F** | 搜索并跳转到节点（自动进入对应子图） |
| **端口拖线** | 从节点右侧输出端口拖到左侧输入端口建边 |
| **单击连线** | 选中边（Delete / X 删除） |
| **边上右键** | 断开连接 / 修改类型 |
| **空白右键** | 创建节点 / 重置视图 |
| **双击项目节点** | 进入该项目的子图 |
| **面包屑** | 在子图层级间导航 |
| **右上角「连线」** | 进入连线模式，依次点击两个节点建立「相关」连接 |
| **右上角 + 节点** | 创建节点（可勾选连接到当前选中） |
| **点击空白** | 取消选中 |
| **Delete / X** | 删除选中的边或节点 |

### 子图（项目层级）

「项目」类节点可作为子图容器：**双击**进入子图，在子图内创建的节点自动归属当前层级。顶部面包屑可返回上级。

示例：双击「Q2 版本」可看到子节点「里程碑 M1」。

### 节点详情面板

选中节点后，右侧面板可：

- **即时编辑**名称与内容（自动保存）
- 查看心理学分类与说明
- **切换分类**（点击分类按钮）
- 查看 **输入/输出连接** 列表，修改类型或断开
- **新建连接**（下拉选择，或从端口拖线）
- **进入子图**（项目类节点）
- **删除节点**（子节点与关联边一并移除）

---

## 快速捕获

用于快速记录一闪而过的想法，并自动创建图谱节点。

**打开方式：**

- 顶部 **「快速捕获」** 按钮
- 快捷键 **`Ctrl+K`**（Mac 为 `Cmd+K`）

**填写项：**

| 字段 | 说明 |
|------|------|
| 关注点标题 | 可选，勾选「同时加入关注区」时必填 |
| 图谱节点名称 | 在图谱中显示的名称（默认同标题） |
| 思维分类 | 概念 / 问题 / 决策 / 项目 / 人物 / 洞察 / 经验 |
| 域 | 研究 / 工作 / 个人 |
| 关联已有节点 | 可选，新建节点会自动连一条「相关」边 |
| 同时加入关注区 | 勾选后，会同时出现在左侧关注区 |

按 **`Esc`** 关闭弹窗。

---

## 数据管理

### 存储位置

所有数据保存在**当前浏览器**的 `localStorage` 中，不会上传到服务器。

### 导出备份

点击顶部 **「导出」** → 下载 JSON 文件，可用于备份或迁移。

### 重置数据

点击 **「重置」** → 恢复为 `data/seed.json` 中的示例数据，**当前修改会丢失**。

### 换浏览器 / 清缓存

数据不会自动同步。换环境前请先 **导出** 备份。

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+K` | 打开快速捕获 |
| `Tab` | 打开创建节点命令面板 |
| `Ctrl+F` | 搜索跳转节点 |
| `Delete` / `X` | 删除选中的边或节点 |
| `Esc` | 关闭弹窗 / 取消连线模式 |

---

## 推荐使用流程

1. **每天早上**：在关注区添加今天的主要关注点（2～4 个）
2. **工作过程中**：用 `Ctrl+K` 快速捕获新想法，关联到已有概念
3. **切换任务时**：点击对应关注卡片，让图谱聚焦到相关思维网络
4. **任务完成**：点 ✓ 标记完成
5. **每周**：导出 JSON 做一次备份

---

## 常见问题

**Q：刷新页面后数据还在吗？**  
A：在，只要没清浏览器缓存或 `localStorage`。

**Q：关注和图谱节点是什么关系？**  
A：关注项是「工作记忆」（你现在在想什么），图谱节点是「语义记忆」（稳定概念）。一个关注可关联多个节点。

**Q：为什么某个关注点排名下降了？**  
A：久未点击会触发衰减。用 📌 固定，或重新点击该关注即可提升活跃度。

**Q：图谱节点能手动连边吗？**  
A：可以。从节点端口拖线、连线模式、节点详情面板下拉建边，或创建节点时勾选「连接到当前选中」。

**Q：如何体验子图？**  
A：双击「Q2 版本」项目节点，或通过 Ctrl+F 搜索「里程碑 M1」自动跳转。

---

## 示例数据说明

首次打开会加载种子数据，包含：

- 3 个关注点：角色渲染优化、Q2 版本排期、思维宫殿搭建
- 8 个思维节点（含 Q2 版本子图下的「里程碑 M1」）及它们之间的关系

可直接点击体验交互，熟悉后再添加自己的内容。
